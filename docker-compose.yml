services:
  concert-dashboard:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped

  # 향후 전시정보 대시보드 서비스 추가 예시:
  # exhibition-dashboard:
  #   build: ./exhibition
  #   ports:
  #     - "5001:5000"
  #   env_file:
  #     - .env
  #   restart: unless-stopped
